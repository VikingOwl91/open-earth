/* P1 exploration workflow: richer USGS historical search + navigable relationship cards. */
(() => {
  const details = document.querySelector('#details'), historyDetails = document.querySelector('.history-search'), start = document.querySelector('#history-start'), end = document.querySelector('#history-end'), minMag = document.querySelector('#history-mag'), radius = document.querySelector('#history-radius'), aroundSelected = document.querySelector('#history-selected'), status = document.querySelector('#history-status'), run = document.querySelector('#history-run');
  const grid = historyDetails?.querySelector('.history-grid');
  const field=(label,id,attrs)=>{const el=document.createElement('label');el.innerHTML=`${label}<input id="${id}" ${attrs}/>`;grid?.append(el);return el.querySelector('input')};
  const maxMag=document.querySelector('#history-max-mag')||field('Max magnitude','history-max-mag','type="number" min="0" max="10" step="0.1" placeholder="Any"'),minDepth=document.querySelector('#history-min-depth')||field('Min depth','history-min-depth','type="number" step="1" placeholder="Any"'),maxDepth=document.querySelector('#history-max-depth')||field('Max depth','history-max-depth','type="number" step="1" placeholder="Any"');
  let scope=document.querySelector('#history-scope');if(!scope&&aroundSelected){const wrap=document.createElement('label');wrap.className='history-scope';wrap.innerHTML='Search area<select id="history-scope"><option value="selected">Around selected feature</option><option value="bounds">Current map bounds</option></select>';aroundSelected.replaceWith(wrap);scope=wrap.querySelector('select')}
  function selectedPoint(){const c=selectedMapFeature?.feature?.geometry?.coordinates;return Array.isArray(c)&&Number.isFinite(+c[0])&&Number.isFinite(+c[1])?[+c[0],+c[1]]:null}function setScope(mode){if(scope)scope.value=mode;if(radius?.closest('label'))radius.closest('label').hidden=mode!=='selected'}scope?.addEventListener('change',()=>setScope(scope.value));setScope(scope?.value||'selected');
  function validateDates(){if(!start?.value||!end?.value)throw new Error('Choose a start and end date.');if(start.value>end.value)throw new Error('Start date must be before end date.')}function addNumber(params,key,input){if(input?.value!==''&&Number.isFinite(Number(input.value)))params.set(key,input.value)}
  historicalSearch=async function historicalSearchEnhanced(){try{validateDates();const params=new URLSearchParams({format:'geojson',starttime:start.value,endtime:end.value,orderby:'time',limit:'20000'});addNumber(params,'minmagnitude',minMag);addNumber(params,'maxmagnitude',maxMag);addNumber(params,'mindepth',minDepth);addNumber(params,'maxdepth',maxDepth);const mode=scope?.value||'selected',pivot=selectedPoint();if(mode==='selected'){if(!pivot)throw new Error('Select a point feature first, or use current map bounds.');params.set('latitude',pivot[1]);params.set('longitude',pivot[0]);params.set('maxradiuskm',radius?.value||'250')}else{const b=map.getBounds();params.set('minlatitude',b.getSouth());params.set('maxlatitude',b.getNorth());params.set('minlongitude',b.getWest());params.set('maxlongitude',b.getEast())}status.textContent='Searching USGS earthquake catalog…';run.disabled=true;const r=await fetch(`https://earthquake.usgs.gov/fdsnws/event/1/query?${params}`);if(!r.ok)throw new Error(`${r.status} ${await r.text()}`);historicalData=await r.json();historicalMode=true;quakeData=historicalData;window.openEarthHistoricalQuery={start:start.value,end:end.value,scope:mode,radius:mode==='selected'?Number(radius?.value||250):null,pivot:mode==='selected'?pivot:null,minMagnitude:minMag?.value||null,maxMagnitude:maxMag?.value||null,minDepth:minDepth?.value||null,maxDepth:maxDepth?.value||null};map.getSource('earthquakes')?.setData(quakeData);document.querySelector('#quake-count').textContent=quakeData.features.length.toLocaleString();document.querySelectorAll('#range button').forEach(b=>b.classList.remove('active'));const area=mode==='selected'?`${radius?.value||250} km radius`:'current map bounds';status.innerHTML=`<strong>${quakeData.features.length.toLocaleString()}</strong> earthquakes · ${esc(start.value)} → ${esc(end.value)} · ${esc(area)}${quakeData.features.length>=20000?'<br>Result limit reached — narrow the query for a complete result set.':''}`;if(selectedMapFeature)detail(selectedMapFeature.layer,selectedMapFeature.feature)}catch(e){console.warn('historical earthquake search failed',e);status.textContent=`Search failed: ${e.message}`}finally{run.disabled=false}};
  if(run){const replacement=run.cloneNode(true);run.replaceWith(replacement);replacement.addEventListener('click',()=>historicalSearch())}
  function relationshipCard(action,valueText,label,extra=''){return `<button type="button" class="relationship-card" data-rel-action="${action}" ${extra}><b>${valueText}</b><span>${label}</span></button>`}function enhancedRelationships(coords){const boundary=typeof boundaryContext==='function'?boundaryContext(coords):(plateData.features.length?nearestLine(coords,plateData):null),plate=typeof containingPlate==='function'?containingPlate(coords):null,fault=faultData.features.length?nearestLine(coords,faultData):null,qs=nearbyQuakes(coords,quakeData,250),strong=qs[0];const tectonic=(boundary||plate)?`<div class="inspector-section tectonic-context"><div class="inspector-heading">Tectonic context</div><div class="context-primary">${plate?`<div><b>${esc(plateName(plate))}</b><span>PB2002 plate</span></div>`:''}${boundary?`<div><b>${esc(boundary.label||'Plate boundary')}</b><span>${boundary.pair?esc(boundary.pair)+' · ':''}${Math.round(boundary.distance)} km away</span></div>`:''}</div></div>`:'';return `${tectonic}<div class="inspector-section"><div class="inspector-heading">Nearby</div><div class="relationship-grid interactive">${relationshipCard('boundary',boundary?`${Math.round(boundary.distance)} km`:'—',boundary?esc((boundary.label||'typed boundary').toLowerCase()):'nearest typed boundary',boundary?'':'disabled')}${relationshipCard('history',`${qs.length}`,`earthquakes / 250 km (${historicalMode?'search':'current'})`,`data-lng="${coords[0]}" data-lat="${coords[1]}"`)}${relationshipCard('strongest',strong?`M ${Number(strong.properties.mag).toFixed(1)}`:'—',strong?`${Math.round(strong._distance)} km away`:'strongest nearby',strong?`data-id="${esc(strong.id||'')}"`:'disabled')}${relationshipCard('fault',fault?`${Math.round(fault.distance)} km`:viewState.layers.faults?'—':'off','nearest active fault',fault?'':'disabled')}</div><p class="relationship-hint">Click a relationship to explore it on the map or use it as a historical-search pivot.</p></div>`}relationships=enhancedRelationships;
  function rerenderSelected(){if(selectedMapFeature)detail(selectedMapFeature.layer,selectedMapFeature.feature)}function openHistoryForSelected(){historyDetails.open=true;setScope('selected');if(radius)radius.value='250';historyDetails.scrollIntoView({block:'nearest',behavior:'smooth'});status.textContent='Ready to search 250 km around the selected feature.'}function selectRelationshipFeature(kind){const c=selectedPoint();if(!c)return;const hit=kind==='boundary'?(typeof boundaryContext==='function'?boundaryContext(c):nearestLine(c,plateData)):nearestLine(c,faultData);if(!hit?.feature)return;if(kind==='boundary'&&typeof showTectonicDetail==='function'&&hit.feature.properties?.Boundary_Family)showTectonicDetail(hit.feature);else selectFeature(kind==='fault'?'faults':'plates',hit.feature);const coords=hit.feature.geometry?.coordinates,first=hit.feature.geometry?.type==='LineString'?coords?.[Math.floor(coords.length/2)]:null;if(first)map.easeTo({center:first,zoom:Math.max(map.getZoom(),6)})}
  details?.addEventListener('click',e=>{const card=e.target.closest('[data-rel-action]');if(!card||card.disabled)return;const action=card.dataset.relAction;if(action==='history')return openHistoryForSelected();if(action==='boundary'||action==='fault')return selectRelationshipFeature(action);if(action==='strongest'){const c=selectedPoint(),quake=c?nearbyQuakes(c,quakeData,250)[0]:null;if(quake){map.easeTo({center:quake.geometry.coordinates,zoom:Math.max(map.getZoom(),7)});selectFeature('earthquakes',quake)}}});rerenderSelected();
})();
