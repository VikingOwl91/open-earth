/* Add PVMBG regional monitoring to the existing rich volcano drawer without conflating it with GVP. */
(() => {
  let current=null,busy=false;
  const body=document.querySelector('#details-body');if(!body)return;
  function card(s){const age=window.pvmbgSnapshotAge?.();return `<section class="drawer-card pvmbg-card" data-pvmbg-card><div class="inspector-heading">Regional monitoring · PVMBG / MAGMA Indonesia</div><div class="meta">${fact('Activity level',`Level ${s.level} · ${s.label}`)}${age?fact('Snapshot',age):''}</div><p class="semantic-note">Official Indonesian volcano activity level. This regional monitoring status is separate from the GVP weekly report and catalog.</p>${s.reportUrl?`<a class="drawer-source-link" href="${esc(s.reportUrl)}" target="_blank" rel="noreferrer">Latest MAGMA report ↗</a>`:''}</section>`}
  function inject(){if(busy||!current)return;busy=true;try{const s=window.pvmbgForVolcano?.(current);if(!s)return;const content=body.querySelector('.drawer-content'),head=body.querySelector('.drawer-head h2');if(!content||!head)return;if(!head.querySelector('[data-pvmbg-badge]'))head.insertAdjacentHTML('beforeend',` <em data-pvmbg-badge>PVMBG · ${esc(s.label.toUpperCase())}</em>`);const active=body.querySelector('.drawer-tabs .active')?.dataset.drawerTab;if(active==='overview'&&!content.querySelector('[data-pvmbg-card]')){const hero=content.querySelector('.poi-hero');if(hero)hero.insertAdjacentHTML('afterend',card(s));else content.insertAdjacentHTML('afterbegin',card(s))}if(active==='sources'&&!content.querySelector('[data-pvmbg-source]'))content.insertAdjacentHTML('beforeend',`<section class="drawer-section" data-pvmbg-source><div class="inspector-heading">Regional monitoring</div><p class="source"><a href="https://magma.esdm.go.id/v1/gunung-api/tingkat-aktivitas" target="_blank" rel="noreferrer">PVMBG / MAGMA Indonesia</a></p>${window.pvmbgSnapshotAge?.()?`<p class="source">PVMBG snapshot: ${esc(window.pvmbgSnapshotAge())}</p>`:''}</section>`)}finally{busy=false}}
  const prior=detail;detail=function(layer,f){if(layer==='volcanoes'||layer==='activity')current=f.properties||null;else current=null;prior(layer,f);queueMicrotask(inject)};
  new MutationObserver(()=>queueMicrotask(inject)).observe(body,{childList:true,subtree:true});
  window.addEventListener('openearth:pvmbg-ready',inject);
})();
